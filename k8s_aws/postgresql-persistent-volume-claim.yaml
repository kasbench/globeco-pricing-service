apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pricing-service-postgres-pvc
  namespace: globeco
  labels:
    app: pricing-service-postgres
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-gp3
  resources:
    requests:
      storage: 10Gi
